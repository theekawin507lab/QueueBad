# 🏸 QueueBad — Patch Notes (บันทึกการปรับปรุงระบบ)

---

## 📌 Version 2.1.0 — Central Queue Pool & 4-Court Standard Update
**วันที่อัปเดต:** 4 กันยายน 2026  
**สถานะ:** Production Ready ✅

---

### 🌟 ฟีเจอร์หลักและการเปลี่ยนแปลงครั้งใหญ่ (Major Features)

#### 1. ⚡ ระบบคิวรวมกลาง (Central Pool Queue & Auto Court Dispatch)
- **ไม่ต้องเดาและเลือกสนามล่วงหน้า:** 
  - ทั้งผู้เล่น (ในหน้า `public.html`) และแอดมิน (ในหน้า `index.html`) สามารถสร้างคิวโดยเลือกการจัดสรรสนามเป็น **"⚡ อัตโนมัติ (ลงคอร์ทแรกที่ว่าง)"**
  - คิวจะถูกส่งเข้าสู่ **"แถวคิวรอลงสนามกลาง (Central Waiting Queue)"** เรียงลำดับตามเวลาและหมายเลขคิวอย่างยุติธรรม (FIFO - First In, First Out)
- **ดึงลงสนามอัตโนมัติทันทีที่คอร์ทว่าง (Zero Idle Time):**
  - เมื่อมีสนามใดสนามหนึ่งแข่งขันจบลง (บันทึกคะแนน) ระบบจะค้นหาคิวแรกใน Central Queue ที่มีผู้เล่นครบ 4 คน และดึงเข้าสู่สนามนั้นทันที
  - เริ่มต้นนับถอยหลังเรียกคิว (CALLING 90 วินาที) โดยอัตโนมัติ
  - หากสร้างคิวที่มีผู้เล่นครบ 4 คนอยู่แล้ว และมีสนามว่างอยู่ ณ ขณะนั้น ระบบจะบรรจุลงสนามว่างและเรียกคิวให้ทันที

#### 2. 🏟️ ล็อคระบบ 4 สนามมาตรฐาน (Fixed 4 Courts System)
- ปรับลดและจำกัดระบบให้อยู่ที่ **4 สนามคงที่ (สนาม 1 ถึง สนาม 4)** อย่างเข้มงวด
- ตัดตัวเลือกสนาม 5 และ 6 ออกจากทุกหน้าจอ (`public.html`, `index.html`, `booking.html`)
- บอร์ดแสดงผล 4 สนามถูกจัดเรียงอย่างสมดุล (Responsive Grid) ดูง่ายทั้งบนจอคอมพิวเตอร์ จอโทรศัพท์มือถือ และจอทีวีสนาม (TV Mode)

#### 3. 🎨 ปรับปรุงการจัดวางหน้า Public View ใหม่ (Public View Redesign)
- **ส่วนที่ 1 (บนสุด): กระดาน 4 สนามแข่งขัน**
  - แสดงการ์ดของสนาม 1 ถึง 4 อย่างเด่นชัด ขนาดเท่ากัน
  - แสดงสถานะ: `กำลังแข่งขัน` (พร้อม Live Timer), `กำลังเรียกคิว` (พร้อม Progress Bar นับถอยหลัง), หรือ `สนามว่าง` (พร้อมรับคิวถัดไป)
- **ส่วนที่ 2 (ตรงกลาง): แถวคิวรอลงสนามกลาง (Central Waiting Queue)**
  - แสดงคิวที่กำลังรอลงสนามทั้งหมด ใครเป็นคิวถัดไป (`🔥 คิวถัดไป`)
  - แสดงสล็อตว่าง และปุ่ม `[ + เข้าร่วม ]` สำหรับผู้เล่นที่ต้องการจอยห้อง
- **ส่วนที่ 3 (ด้านล่าง): ผู้เล่นที่พร้อมลงสนาม (Present Players - Compact & Collapsible)**
  - ย้ายรายชื่อผู้เล่นที่มาสนามจากแถบด้านข้างลงมาไว้ด้านล่าง เพื่อคืนพื้นที่หน้าจอหลักให้กระดานสนาม
  - แสดงผลในรูปแบบ **ชิปแคปซูลขนาดกะทัดรัด (Compact Chips)** พร้อมจุดไฟสถานะออนไลน์สีเขียวกระพริบ
  - มีปุ่ม **"พับเก็บ / แสดง"** พับเก็บได้ใน 1 คลิก ไม่เปลืองพื้นที่การเลื่อนหน้าจอบนมือถือ
  - เพิ่ม **ช่องค้นหาชื่อผู้เล่น** ค้นหาได้ทั้งชื่อเล่นและชื่อจริงแบบ Real-time

---

### 🛠️ รายการแก้ไขบัคและปรับปรุงความเสถียร (Bug Fixes & Hardening)

1. **แก้ปัญหาแอดมินลบรายชื่อผู้เล่นแล้วรายชื่อเด้งกลับมา (Player Resurrection):**
   - *สาเหตุ:* โค้ดเดิมลบเฉพาะในเครื่อง แต่ไม่ได้ส่งคำสั่ง `.delete()` ไปยัง Cloud Firestore ทำให้ Real-time Snapshot ดึงรายชื่อเดิมกลับมา
   - *การแก้ไข:* อัปเดต `removePlayer()` ให้ลบเอกสารใน Firestore ออกถาวร และปรับปรุง `syncPlayersToCloud()` ให้ตรวจเช็คและลบข้อมูลที่ไม่อยู่ในระบบออกอัตโนมัติ
2. **แก้ปัญหา Public View เกิดข้อผิดพลาด/หน้าจอค้าง:**
   - *สาเหตุ:* การแปลงเวลาโดยตรง `.toLocaleTimeString()` บนค่าว่างหรือรูปแบบผิดปกติ ทำให้เกิด `RangeError: Invalid time value` และการดึงสล็อตว่างที่ไม่มีอาเรย์ทำให้เกิด `TypeError`
   - *การแก้ไข:* เพิ่มฟังก์ชัน `formatTimeSafe()` ตรวจสอบความถูกต้องของ Date ก่อนแปลง และเพิ่ม Safe Null Navigation `(m.teamA && m.teamA[0]) || '(ว่าง)'` ทุกจุด
3. **แก้ปัญหากด "บันทึกคะแนน" ใน Index.html แล้วไม่มีอะไรเกิดขึ้น:**
   - *สาเหตุ:* ฟังก์ชัน `openScoreModal` และ `confirmScore` เปรียบเทียบ `m.id === id` แบบ Strict Equality ซึ่งค่า ID มีทั้งประเภท String และ Number ทำให้หาคิวไม่เจอและ JS เกิด Error
   - *การแก้ไข:* ปรับการเปรียบเทียบ ID ทุกฟังก์ชันเป็น `String(m.id) === String(id)` พร้อมป้องกันค่า `playDuration` ไม่ให้คำนวณเป็น `NaN`
4. **แก้ปัญหาคิวเริ่มต้นที่คิวที่ 2 และเพิ่มเครื่องมือจัดเรียงคิว:**
   - *การแก้ไข:* เพิ่มฟังก์ชันและปุ่ม **`[ 🔄 จัดเรียงเลขคิวใหม่ (เริ่มจาก 1) ]`** บนหัวตารางคิวในหน้า Admin ให้แอดมินสามารถกดรีเซ็ตเลขคิวทั้งหมดให้เริ่มนับ 1 ใหม่ได้ตลอดเวลาในคลิกเดียว

---

### 📂 สรุปไฟล์ที่มีการเปลี่ยนแปลง (Files Modified)

| ไฟล์ | รายละเอียดการเปลี่ยนแปลง |
| :--- | :--- |
| `public.html` | ปรับ Layout ใหม่ 3 ชั้น (4 สนาม -> คิวกลาง -> ผู้เล่นกะทัดรัดด้านล่าง), ปรับตัวเลือกสนามใน `createQueueModal` เป็น อัตโนมัติ และ 1-4 |
| `js/public.js` | จำกัด `TOTAL_COURTS = 4`, ปรับปรุง `renderDashboard()` แยกการ์ดสนามและ Central Queue, เพิ่ม `renderOnlinePlayers()` แบบชิปพร้อม Search & Collapse, ปรับปรุง `submitCreateQueue()` และ `submitJoinSlot()` รองรับ Auto Court Dispatch |
| `index.html` | เปลี่ยน input เลขสนามเป็น dropdown เลือก 4 สนามและอัตโนมัติ, เพิ่มปุ่มจัดเรียงเลขคิวใหม่ (เริ่มจาก 1) |
| `js/index.js` | รองรับการสร้างคิวอัตโนมัติ, ปรับปรุง `autoCallNextQueue()` ให้ดึงคิวกลางเมื่อสนามว่าง, ปรับปรุง `editCourt()` ให้จำกัด 1-4 หรือคิวกลาง, เพิ่มฟังก์ชัน `assignCourtToMatch()` |
| `booking.html` | ปรับตัวเลือกสนามเหลือสูงสุด 4 สนาม (สนาม 1 - 4) |
| `PATCH_NOTES.md` | เอกสารบันทึกการเปลี่ยนแปลงของระบบ (สร้างใหม่) |

---

### 📖 คู่มือการใช้งานสำหรับผู้ใช้ (User Guide)

#### สำหรับผู้เล่น (Mobile / Public View):
1. **การดูคิว:** เข้าหน้า `public.html` จะเห็นสถานะสนาม 1-4 อยู่ด้านบน และแถวคิวรอด้านล่าง
2. **การสร้างคิว:** กดปุ่ม **`[ 🏸 สร้างคิว / เปิดห้อง ]`** -> ช่องสนามจะถูกเลือกเป็น **"⚡ อัตโนมัติ"** โดยค่าเริ่มต้น -> ระบุเพื่อนร่วมทีม -> กดยืนยัน คิวจะไปต่อท้ายในคิวกลางทันที และเมื่อมีสนามว่างระบบจะเรียกคิวให้
3. **การเช็คผู้เล่นที่สนาม:** เลื่อนลงมาด้านล่างสุดจะพบรายชื่อผู้เล่นที่เช็คชื่อมาสนาม สามารถพิมพ์ค้นหาชื่อ หรือกดพับเก็บได้

#### สำหรับผู้ดูแลระบบ (Admin Dashboard):
1. **การเพิ่มคิว:** ในหน้า `index.html` เลือกลงสนามแบบ **"อัตโนมัติ (คอร์ทแรกที่ว่าง)"** ได้ทันที หากมีสนามว่างระบบจะส่งลงสนามนั้นทันที
2. **การจบเกม:** กด **"บันทึกคะแนน"** -> กรอกแต้ม -> บันทึก ระบบจะจบเกมและ**ดึงคิวถัดไปจากคิวกลางลงสนามนั้นให้ทันที**
3. **การจัดสนามด้วยตนเอง (Manual Override):** หากต้องการย้ายคิวใดลงสนามเฉพาะเจาะจง สามารถคลิกที่ปุ่มสนามของคิวนั้นเพื่อเปลี่ยนเป็นสนาม 1, 2, 3 หรือ 4 ได้ตลอดเวลา

---

## [2.1.1] - 2026-09-04 (Emoji Reduction & UI Polish)

### 🎨 ปรับปรุงความเรียบหรูและลดการใช้อิโมจิที่ไม่จำเป็น (Emoji Overhaul)
- **หลักการออกแบบใหม่:** ตัดอิโมจิประดับตกแต่งทั้งหมดในหัวข้อ บัตร ป้ายชื่อ เมนู ตัวเลือกแจ้งเตือน (Alerts) และปุ่มกด เพื่อให้หน้าจอมีความสุขุม สะอาดตา ระดับมืออาชีพ (Clean & Minimalist Enterprise Look) โดยคงไว้เฉพาะสัญลักษณ์เชิงฟังก์ชันที่จำเป็น เช่น สวิตช์สลับธีม (`🌙`)
- **การปรับปรุงรายหน้า:**
  1. **`public.html` & `js/public.js`:**
     - ลบอิโมจิออกจากแถบนำทาง (Navbar), แบนเนอร์สถานะ, และหัวข้อทุก Section
     - เปลี่ยนป้ายการ์ดสนามว่างจากการใช้อิโมจิไม้แบด เป็นป้ายวงกลมข้อความ `ว่าง` สไตล์โมเดิร์น
     - ในกล่องคิวกลาง นำอิโมจิประกายวิบวับออก แสดงข้อความสถานะชัดเจน
     - เปลี่ยนปุ่มลอยมือถือ (FAB) จากอิโมจิเป็นเครื่องหมาย `+` คลีน
     - ใน Drawer โปรไฟล์ และ Modal: ตัดอิโมจิหัวข้อและตัวเลือกสล็อต
     - ข้อความแจ้งเตือน (`alert`, `confirm`): ลบอิโมจิแจ้งเตือนทั้งหมด เช่น `🎉`, `⚠️`, `🗑️`, `🚪`, `🏖️`
  2. **`index.html` & `js/index.js`:**
     - ลบอิโมจิ `⚡` ในหัวข้อ Cloud Connected, ตัวเลือกคอร์ท และปุ่มจัดลงสนามว่าง
     - เปลี่ยนสถานะรายชื่อผู้เล่นจาก `✅ / ❌` เป็น Badge ข้อความ `พร้อม` (สีเขียว) และ `ไม่พร้อม` (สีเทา) สวยงาม อ่านง่าย
     - ปรับปรุงแท็บและเครื่องมือจัดการคิว ตัดอิโมจิหน้าชื่อแท็บออกทั้งหมด
     - เปลี่ยนปุ่มสลับตัวผู้เล่นจาก `✏️` เป็นสัญลักษณ์แก้ไข `✎`
  3. **`login.html` & `register.html`:**
     - เปลี่ยนอิโมจิหัวหน้ากระดาษเป็นตราสัญลักษณ์ `UP` เรียบหรู
     - ตัดอิโมจิประดับในหัวข้อย่อยแบบฟอร์ม (`🔐`, `🏷️`, `👤`, `✨`)
  4. **`booking.html`:**
     - นำอิโมจิในหัวข้อแบบฟอร์มออกให้ดูเป็นทางการสำหรับการติดต่อราชการ

---

## [2.1.2] - 2026-09-04 (Google Auth, Persistent Login & UI Refinements)

### 🚀 ฟีเจอร์ใหม่และการปรับปรุงความเสถียร (New Features & Enhancements)

#### 1. 🔒 ระบบจดจำผู้ใช้งานถาวร (Persistent Login & Auto Routing)
- **จำสถานะข้ามการรีเฟรชและเปิดแท็บใหม่:** แก้ปัญหาเดิมที่เก็บข้อมูลเฉพาะใน `sessionStorage` ทำให้ต้องเข้าสู่ระบบใหม่ทุกครั้งเมื่อรีเฟรชหน้าต่าง
- **สลับการบันทึกสู่ `localStorage`:** บันทึกข้อมูลโปรไฟล์และสถานะการเข้าสู่ระบบลงใน `localStorage` ควบคู่กับ `sessionStorage`
- **ตั้งค่า Firebase Auth Persistence:** กำหนดเป็น `firebase.auth.Auth.Persistence.LOCAL` ช่วยรักษาโทเค็นการยืนยันตัวตนไว้ในเบราว์เซอร์
- **ระบบ Auto-Redirect เมื่อกลับมาหน้า Login:** หน้า `login.html` มีการตรวจจับเซสชันเดิมอัตโนมัติ หากผู้ใช้เคยเข้าสู่ระบบไว้แล้ว ระบบจะเปลี่ยนเส้นทางไปยัง Dashboard (`index.html` สำหรับผู้ดูแล หรือ `public.html` สำหรับผู้เล่น) โดยอัตโนมัติ ไม่ต้องกรอกรหัสซ้ำ
- **เพิ่มตัวเลือก "จดจำการเข้าสู่ระบบ" (Remember Me):** ผู้ใช้สามารถเลือกเปิด/ปิดการจดจำได้ตามต้องการ
- **อัปเดตระบบ Logout ทุกหน้าจอ:** เมื่อกดออกจากระบบ ระบบจะล้างข้อมูลทั้ง `localStorage`, `sessionStorage` และเรียก `auth.signOut()` อย่างหมดจด

#### 2. 🌐 ระบบเข้าสู่ระบบด้วยบัญชี Google (Google Sign-In)
- **ล็อกอินสะดวกในคลิกเดียว:** เพิ่มปุ่ม "เข้าสู่ระบบด้วย Google" บนหน้า `login.html` รองรับการเข้าสู่ระบบผ่าน Google Provider ของ Firebase Authentication
- **สร้างโปรไฟล์และลงชื่อคิวอัตโนมัติ:** เมื่อล็อกอินด้วยบัญชี Google เป็นครั้งแรก ระบบจะสร้างข้อมูลผู้ใช้ (Role: `player`) และลงทะเบียนในรายชื่อผู้เล่นที่สนาม (`players`) ให้โดยอัตโนมัติ
- **ระบบจัดการข้อผิดพลาดครบถ้วน:** แสดงข้อความแจ้งเตือนภาษาไทยที่เข้าใจง่าย เช่น เมื่อผู้ใช้ปิดหน้าต่างป๊อปอัป หรือกรณีการอนุญาตสิทธิ์

#### 3. 🎨 ปรับปรุงสีปุ่มแถบนำทาง (High-Contrast Header Buttons)
- **แก้ปัญหาปุ่มกลืนกับพื้นหลัง:** ในหน้า `public.html` แถบนำทางด้านบนเป็นสีน้ำเงินเข้ม (`bg-blue-700`)
- **ปุ่ม "คู่มือ":** เปลี่ยนเป็นสีเขียวมรกตเด่นชัด (`bg-emerald-600 hover:bg-emerald-500`) สอดคล้องกับหน้า Admin และ Booking
- **ปุ่ม "ขอใช้สนาม":** เปลี่ยนเป็นสีม่วงโดดเด่น (`bg-purple-600 hover:bg-purple-500`) ให้เห็นชัดเจนและตัดกับแถบนำทาง

#### 4. 🏟️ ปรับปรุงกรอบสถานะ 4 สนามแข่งขัน และลบป้ายซ้ำซ้อน
- **เพิ่มกรอบคอนเทนเนอร์:** ห่อหุ้มโซน "สถานะสนามแข่งขัน (สนาม 1 - 4)" ใน `public.html` ด้วยกล่อง Card สีขาว/ดาร์กโมด (`rounded-2xl p-5 sm:p-6 shadow-sm border border-slate-200 dark:border-slate-700`) มีเส้นขอบแบ่งสัดส่วนชัดเจนระดับพรีเมียม สอดรับกับโซนคิวกลาง
- **ตัดป้าย "ระบบ 4 สนาม" ออก:** ลบป้ายข้อความที่ไม่จำเป็นออกเพื่อความสะอาดตาและเป็นระเบียบ
- **เพิ่มความคมชัดของการ์ดสนาม:** ปรับขอบของการ์ดสนามแต่ละคอร์ทให้มีเส้นขอบ `border-2` คมชัด ดูเด่นสะดุดตา

#### 5. ✂️ ปรับแต่ง Modal สร้างห้อง (Create Queue Modal)
- **ตัดปุ่มทางลัดที่ไม่จำเป็น:** ลบปุ่ม "มาคนเดียว (เปิดว่าง 3 ที่)" และ "มีคู่แล้ว (เปิดว่างทีม B)" ออก เนื่องจากผู้สร้างคิวสามารถเลือกรายชื่อเพื่อนหรือเว้นว่างได้เองอย่างยืดหยุ่น
- **ลดทอนข้อความใน Dropdown:** ปรับตัวเลือกเริ่มต้นของช่องเลือกเพื่อนร่วมทีม จาก `-- (ว่าง - เปิดให้คนอื่นเข้าร่วม) --` ให้เหลือเพียงคำว่า **"ว่าง"** เพื่อความกระชับ สบายตา และอ่านง่ายขึ้น

#### 6. 📋 ปรับปรุงระบบลงทะเบียนผู้เล่นใหม่ (Registration Overhaul)
- **เพิ่มช่อง Username:** ผู้ลงทะเบียนสามารถตั้ง Username (ภาษาอังกฤษ/ตัวเลข/ขีดล่าง ความยาว 3-30 ตัว) เพื่อใช้เป็นชื่อบัญชีสำรองในการเข้าสู่ระบบ (บันทึกลง Firestore field `username`)
- **ลดทอน Label ชื่อเล่น:** เปลี่ยนจาก "ชื่อเล่น (Nickname / Display Name)" ให้เหลือเพียง "ชื่อเล่น (Nickname)" กระชับและสะอาดตาขึ้น
- **เปลี่ยน "ชั้นปี" เป็น "สถานะผู้ใช้":** ย้ายขึ้นมาอยู่เหนือฟิลด์คณะ/สาขา พร้อมปรับตัวเลือกใหม่ให้ครอบคลุมทุกกลุ่มผู้ใช้งาน ได้แก่: นักเรียน, นิสิต/นักศึกษา, บัณฑิตศึกษา (ป.โท), บัณฑิตศึกษา (ป.เอก), บุคลากร/เจ้าหน้าที่มหาวิทยาลัย, และบุคคลทั่วไป
- **เพิ่ม Dropdown "กลุ่มวิชาที่ศึกษา":** ให้ผู้ลงทะเบียนเลือกกลุ่มวิชาก่อน เพื่อกรองข้อมูลคณะและสาขาวิชาให้เหลือเฉพาะที่สัมพันธ์กัน (วิทยาศาสตร์สุขภาพ / วิทยาศาสตร์และเทคโนโลยี / มนุษยศาสตร์และสังคมศาสตร์)
- **ระบบ Dropdown Cascade คณะ → สาขาวิชา:** เปลี่ยนจากช่องกรอกข้อความเป็น Dropdown ที่เชื่อมโยงกัน (กลุ่มวิชา → คณะ → สาขาวิชา) บรรจุข้อมูลหลักสูตรครบถ้วน **18 คณะ/วิทยาลัย** ของมหาวิทยาลัยพะเยา ปีการศึกษา 2569
- **แสดงฟิลด์ตามสถานะ:** ฟิลด์คณะ/สาขาวิชาจะแสดงเฉพาะเมื่อสถานะผู้ใช้เป็นนิสิต/นักศึกษา หรือบัณฑิตศึกษา ผู้ใช้ทั่วไปหรือบุคลากรไม่จำเป็นต้องกรอก
- **บันทึกข้อมูลใหม่ใน Firestore:** field `year` (เดิม) เปลี่ยนเป็น `userStatus`, เพิ่ม `username`, `fieldGroup`, `faculty`, `major` เข้าใน collection `users`

---

## [2.1.4] - 2026-09-04 (Report Image Attach, Score-free Match End, Edit Player Profile)

### 🚀 ฟีเจอร์ใหม่
1. **แนบรูปภาพในระบบรายงานปัญหา (Optional):** แนบรูปได้สูงสุด 3 รูป พร้อมหน้าต่าง Preview และปุ่มลบ
2. **จบเกมและบันทึกผลได้ทันทีโดยไม่ต้องใส่คะแนน:** หากไม่ระบุคะแนน ระบบจะบันทึกเป็น 0 - 0 ให้อัตโนมัติ
3. **ระบบแก้ไขโปรไฟล์ผู้เล่น (Edit Player Profile):** ปรับปรุง Drawer ในหน้า Public View ให้มีปุ่ม "แก้ไขข้อมูลส่วนตัว" ปรับปรุงข้อมูล ชื่อเล่น, ชื่อ-สกุล, เบอร์โทร, รหัสนิสิต, สถานะ, คณะ, สาขาวิชา ได้อย่างครบถ้วน

---

## [2.1.5] - 2026-09-04 (Deep Player Deletion, Visual Highlights, Cloud Reports & Flexible Admin Queue)

### 🚀 ฟีเจอร์และการปรับปรุงหลัก

#### 1. 🗑️ ลบข้อมูลผู้เล่นออกจากฐานข้อมูลทั้งหมด (Deep Player Deletion)
- **ลบข้อมูลครบทุกคอลเลกชัน:** ปุ่ม "ลบ" ในหน้ารายชื่อผู้เล่น Admin Dashboard จะลบข้อมูลผู้เล่นออกจากทั้งคอลเลกชัน `players` และ `users` ใน Firestore อย่างสมบูรณ์
- **ค้นหาครอบคลุมทุกมิติ:** ค้นหาและลบเอกสารที่ตรงกับทั้ง `uid`, `nickname`, `username`, และ `firstName`
- **ระบบ Re-Registration Recovery:** ปรับปรุง `register.js` ให้ตรวจสอบหากผู้ใช้ที่ถูก Admin ลบข้อมูลออกจากฐานข้อมูลกลับมาลงทะเบียนใหม่ด้วยอีเมลเดิม ระบบจะอนุญาตให้ลงทะเบียนโปรไฟล์ใหม่ได้อย่างราบรื่น

#### 2. 🟢 Public View ไฮไลต์ชื่อผู้ใช้ และปรับสีทีม B ให้เป็นเอกภาพ
- **ไฮไลต์ชื่อตนเองเป็นสีเขียว:** ชื่อของผู้ใช้เองในคิว (`currentNickname`) ทั้งใน 4 สนาม และในแถวคิวรอกลาง (Central Waiting Queue) จะแสดงเป็นสีเขียวมรกตเด่นชัด (`text-emerald-600 dark:text-emerald-400 font-extrabold`) พร้อมกรอบไฮไลต์และป้าย `👤 (คุณ)`
- **ปรับสีทีม B ให้เป็นสีเดียวกับทีม A:** เปลี่ยนสีชื่อทีม B จากเดิมที่เป็นสีแดง ให้เป็นสีน้ำเงินมาตรฐานแบบเดียวกับทีม A (`text-blue-600 dark:text-blue-400 font-bold`) เพื่อให้สังเกตตำแหน่งชื่อของตนเองได้ง่ายขึ้น ไม่สับสนกับสีแดงของทีม B เดิม
- **แก้ไขบั๊กตัวแปร Calling Match:** แก้ไขข้อผิดพลาดการอ้างอิงตัวแปร `cB0` / `cB1` ให้แสดงผลชื่อผู้เล่นทีม B ได้ถูกต้องเมื่ออยู่ในสถานะกำลังเรียกคิว

#### 3. ☁️ ระบบรายงานปัญหา Cloud Sync ข้ามอุปกรณ์ (Cross-Device Problem Reports)
- **บันทึกลง Cloud Firestore:** ปรับปรุง `js/report.js` ให้ส่งรายงานปัญหาตรงเข้า Firestore คอลเลกชัน `reports` ทำให้รายงานปัญหาจากผู้เล่นทุกคนแสดงผลในกล่องข้อความของ Admin ข้ามอุปกรณ์ได้ทันทีแบบ Real-time
- **ระบบบีบอัดรูปภาพก่อนส่ง (Image Compression):** บีบอัดภาพด้วย HTML5 Canvas ก่อนแปลงเป็น Base64 เพื่อให้ขนาดไฟล์เล็ก ไม่เกินขีดจำกัดของเอกสาร Firestore (1MB)
- **กล่องข้อความ Admin อัปเดต Real-time:** เพิ่ม Real-time Snapshot Listener ในหน้า Admin ดึงรายงานปัญหามาแสดงผลทันทีที่มีผู้ใช้ส่งเข้ามา
- **พรีวิวภาพขนาดย่อ + กล่องขยายดูภาพ (Image Lightbox Modal):** แสดงภาพ Thumbnail ขนาดย่อใต้รายงานปัญหา และสามารถคลิกที่ภาพเพื่อเปิดดูรูปภาพขนาดใหญ่ได้

#### 4. 👥 รองรับการจัดคิวผู้เล่นน้อยกว่า 4 คนใน Admin (2-Player Queue & Auto Dispatch)
- **ปลดล็อคช่องกรอก Required:** ปลดแอตทริบิวต์ `required` ออกจากช่องกรอกผู้เล่นทั้ง 4 ช่องใน `index.html` เพื่อให้เลือกจำนวนผู้เล่นได้อย่างอิสระ
- **แก้ปัญหาแจ้งเตือนชื่อซ้ำ:** แก้ไขอัลกอริทึมการตรวจสอบชื่อซ้ำให้กรองเฉพาะช่องที่มีชื่อผู้เล่นจริง (`filledPlayers`) ป้องกันไม่ให้ค่าว่าง (`""`) ถูกมองเป็นชื่อซ้ำ
- **ระบบจัดทีมเดี่ยว 1 vs 1 อัตโนมัติ:** กรณีเลือกผู้เล่น 2 คนในทีม A ระบบจะจัดผู้เล่นคนที่สองไปอยู่ทีม B อัตโนมัติ เพื่อให้เป็นแมตช์เดี่ยว 1 vs 1
- **คลิกเลือกสล็อตที่ต้องการลงชื่อได้:** Admin สามารถคลิกที่ช่อง `p1`, `p2`, `p3`, `p4` เพื่อระบุช่องที่ต้องการใส่ชื่อได้โดยตรง พร้อมไฟไฮไลต์ Focus
- **ดึงคิว 2 คนลงสนามว่างอัตโนมัติ:** ปรับปรุงฟังก์ชัน `autoCallNextQueue()` ให้ดึงคิวที่มีผู้เล่นตั้งแต่ 2 คนขึ้นไปลงสนามว่างและเริ่มเรียกคิวได้ทันที

#### 5. 🧹 กวาดล้างรายชื่อตัวอย่างเก่า (วา-ขาจร, ยันต์69, คริสตัน, ชัยโรงสี) ออกจากระบบถาวร
- **ตัด Hardcoded Fallback ออก:** ลบรายชื่อตัวอย่างที่เคยฝังไว้ในค่าเริ่มต้นของ `index.js` ออกทั้งหมด ไม่ให้เด้งกลับมาอีกเมื่อเข้าสู่ระบบจากเครื่องใหม่หรือเบราว์เซอร์อื่น
- **ระบบ Auto-Purge จาก Cloud Firestore:** เพิ่มฟังก์ชัน `purgeLegacyDummyPlayers()` ตรวจจับและลบชื่อตัวอย่างทั้ง 4 ชื่อออกจาก Firestore คอลเลกชัน `players` และ `users` อัตโนมัติ
- **กรองรายชื่อในทุกหน้าจอ:** กรองรายชื่อทั้ง 4 ชื่อออกจาก `public.js` และ `booking.js` ไม่ให้แสดงในรายชื่อผู้เล่นที่พร้อมลงสนามอีกต่อไป
---

## [2.1.6] - 2026-09-04 (User Profile Field Group & Major Fix, Full Major Cascade in Profile Edit)

### 🐛 การแก้ไขข้อผิดพลาดและการปรับปรุง (Bug Fixes & Improvements)

#### 1. 🎓 แสดงผลกลุ่มวิชา, คณะ, สาขาวิชา, และสถานะผู้ใช้ ในโปรไฟล์ผู้เล่นครบถ้วน
- **แก้ปัญหาไม่แสดงกลุ่มวิชาและสาขา:** เพิ่มการอัปเดต `#profileFieldGroup` และตรวจสอบ `#profileMajor` ในฟังก์ชัน `populateProfileDrawer()` ของ `js/public.js`
- **รองรับสถานะผู้ใช้ใหม่:** แสดงผล `profile.userStatus` (เช่น นิสิต/นักศึกษา, บัณฑิตศึกษา, บุคคลทั่วไป) ในตำแหน่ง "สถานะผู้ใช้" (`#profileYear`) และป้ายบทบาท (`#profileRole`) อย่างถูกต้อง
- **Cloud Profile Sync:** ซิงค์ข้อมูลล่าสุดจาก Firestore คอลเลกชัน `users` อัตโนมัติเมื่อโหลดหน้าเว็บและเมื่อเปิด Profile Drawer เพื่อให้ข้อมูลสดใหม่อยู่เสมอ แม้จะมีการเปลี่ยนแปลงจากอุปกรณ์อื่นหรือลงทะเบียนใหม่

#### 2. 📚 ระบบเลือกสาขาวิชาในหน้าแก้ไขโปรไฟล์ (Full Curriculum Cascade)
- **เพิ่มฐานข้อมูลหลักสูตร ม.พะเยา ฉบับสมบูรณ์ (`UP_CURRICULUM`):** เพิ่มโครงสร้างข้อมูลกลุ่มวิชา คณะ และสาขาวิชาครบถ้วนทุกหลักสูตรใน `js/public.js`
- **ระบบเลือกสาขาวิชาแบบ Cascade:** เมื่อเลือกคณะใน Dropdown ระบบจะสร้างตัวเลือกสาขาวิชาใน Dropdown `#editMajor` ให้เลือกได้ทันที
- **Pre-populate ข้อมูลเดิมเมื่อเปิดหน้าต่างแก้ไข:** ดึงข้อมูลกลุ่มวิชา คณะ และสาขาเดิมของผู้ใช้มาแสดงใน Dropdown อย่างถูกต้อง
- **แก้ไขข้อผิดพลาด ReferenceError:** แก้ไขการเรียกฟังก์ชัน `renderProfileDrawer` ที่ไม่มีอยู่จริง ให้เป็น `populateProfileDrawer()` ทำให้บันทึกโปรไฟล์สำเร็จ ไม่ค้าง และแจ้งเตือนเสร็จสิ้นอย่างสมบูรณ์

